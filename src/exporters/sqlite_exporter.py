"""This module exists as a placeholder to document that SQLite persistence is handled elsewhere. It keeps the exporters package complete for future extension. Possible improvement: implement a formal SQLite export or remove the placeholder, but time did not allow it."""

# SQLite persistence is handled directly by storage.py