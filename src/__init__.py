"""This package exists to group the core scraper modules into a single importable namespace. It does not execute logic by itself but marks the package boundary used by the CLI and UI. 
Possible improvement: expose a small public API or version metadata, but that is out of scope for this prototype."""

